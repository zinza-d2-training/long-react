import { Box } from '@mui/material';

interface IProps {
  onNextStep: () => void;
  onBackStep: () => void;
}

const Step2 = (props: IProps) => {
  return <Box>Step2</Box>;
};

export default Step2;
