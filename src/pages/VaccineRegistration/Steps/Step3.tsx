import { Box } from '@mui/material';

interface IProps {
  onNextStep: () => void;
  onBackStep: () => void;
}

const Step3 = (props: IProps) => {
  return <Box>Step3</Box>;
};

export default Step3;
