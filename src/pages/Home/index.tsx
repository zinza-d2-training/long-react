import { Box } from '@mui/material';
import { HeaderLayout } from 'theme/layout';

const Home = () => {
  return (
    <HeaderLayout>
      <Box>Home page</Box>
    </HeaderLayout>
  );
};

export default Home;
